Day18
1. Used Sequelize `where` condition with `findOne()` to retrieve a specific product from the database based on its id.
2. Used Sequelize `findAll()` to fetch all products from the database and render them on the admin products page.
3. 