Day 16
1. Connected Node.js app to MySQL using mysql2 connection pool with promise-based queries for efficient and reusable database operations.
2. Made a SQL DB in MySQL and performed CRUD by creating new table and column
3. Retrieved data from products table using db.execute() with promise-based handling of results and errors.
4. Fetched products from MySQL using model method fetchAll() and rendered data in controller using promise response.
5. Inserted new product using INSERT query and fetched single product using WHERE condition with parameterized queries.